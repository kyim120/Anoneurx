import { ReactNode } from "react";

const OSPage = ({ children }: { children: ReactNode }) => (
  <div className="mx-auto max-w-7xl px-4 py-14 md:px-8 md:py-20">{children}</div>
);

export default OSPage;
